# 第二大脑桌面客户端 · 全面交接文档 2026-09-08(晚)

> 三端真机测试+修bug+构建交付,以及新任务(会员价/免费10天)。新会话从这里完整接手。
> ★凭据(密码/火山key/IP)见本地 memory `compound_desktop_realmachine_test_2026-09-07`,或问用户。

## 一、任务与策略
三端真机测 compound-desktop(第二大脑客户端)、揪bug、统一修、统一构建交付。三机:
- **本机 Mac** 8G Intel · lite · 本地(app 在 /Applications/Compound.app,TCC下 ls 可能空、用 open 正常)
- **mac2** 16G Intel · macOS 12.7.6 · HD高精版(带paddle) · SSH zhaojue@172.16.16.172
- **Windows** 8G · lite · SSH Qingshi@172.16.17.175
- 策略:攒bug→统一改→统一构建→三端同步装。用户铁律:不为老机器牺牲好机器。

## 二、代码状态(main 分支)
- 最新 commit=**0351db0**(#8 rundll32 + #9 雷达导出居中)。**已 push,但三端构建被用户叫停取消了**(要先等 ceshi 复杂图测试结论,再决定是否连 ceshi 策略一起构建)。
- 提交线:d06a0fd(#1-#4)→cf87112(#6v1+#7)→f57a0ba(#6v2)→abc0b86(#6闭包)→**82f3250(#6真修 ingest.py)**→a5bd051(#6 curl600s)→**0351db0(#8/#9)**
- ⚠️ **0351db0 代码在 main 但未成功构建**(构建被取消)。要验 #8/#9 需重新构建三端。

## 三、所有 bug 状态
| # | bug | 状态 | 关键 |
|---|---|---|---|
| #1 | 填key不保存直测 | ✅真机验证 | ok:true |
| #2 | 豆包模型下拉 | ✅ | 火山4模型通;10厂商可自填(ModelCombo)+测连通+不存在友好报错;model=质量档 fast=快速档(文档要建议客户pro质量/flash快速) |
| #3 | bg-analyze空转 | ✅ | 无format错误 |
| #6 | HD图片没走paddle | ✅简单图真修(82f3250) | 见§六。简单表格图 method=ocr:paddle+`<table>`;复杂大图见§七 |
| #7 | 开客户端烧CPU300% | ✅真机验证 | 本机300%→0.3%;msg_count持久化列 |
| #8 | Win支付宝打不开支付页 | 🔧代码改0351db0待构建验 | explorer→cmd start(&截断missing-method)→**rundll32**(最新,原样传含&URL) |
| #9 | Win姻缘雷达导出PNG不居中 | 🔧代码改0351db0待构建验 | svg width:100%撑满→margin auto无法居中→**CSS固定340px+margin auto**(去svg width属性) |
| #5 | 微信助手向导钥匙数显示 | ❌未修 | 显示"抓到1把"(本次候选)而非累计19把;数据没丢只是显示口径;~/wxkeys/panel.py capture()/_wpoll |

## 四、★新任务(用户要另一对话做)
1. **会员价格改为"正常价格"** + **注册用户免费用 10 天**
   - ★**会员价/套餐在云端 106 账号中心**:客户端 `GET /api/plans`(app.py:2825)只 `_cloud_proxy("GET","/account/plans")` 转发。**改价改云端 /account/plans 返回值**。
   - 前端 Paywall.jsx:84 有兜底默认 `[{id:'year',amount:199},{id:'month',amount:29}]`(云端没返回时用),可选同步。
   - **免费10天试用**:门控在 app.py:2709 `_gate`(试用过期+未付费→402);trial_until/tier_until 来自**云端 account**。给注册用户10天试用=**改云端**(注册时设 trial_until=注册+10天)。客户端 phone_register(app.py:3763)只 `_cloud_post("/account/sms/login")` 建号,试用期云端发。
   - 结论:这个任务**主要改云端账号中心(106.189)**,不是 compound-desktop 仓库。

## 五、已真机验证通过(有据)
装机三端/微信助手下载/入库全品种9格式(txt/md/csv/html/eml/docx/xlsx/pptx/png,本机+mac2+Windows)/搜索(FTS+高亮+OCR/office)/问答RAG/账号全链(注册登录改密忘记密码token吊销错误拒登,测试号17274630436)/保存设置/密码强度/多用户好友(请求同意互为好友)/姻缘needs_persona引导/多厂商分析(火山pro+deepseek)/探索发现文库卡片today

## 六、★★★#6 完整真相(耗时最久,务必读)
图片单张入库(IMG_EXTS)走 **ingest.py 的 process_image**(process_any line342 调),它源码**写死RapidOCR、无paddle分支**;前几轮一直改 media_ingest 全**改错模块**。真修(82f3250):ingest.py.process_image 加paddle分支—读 sidecar_main 落盘的 worker URL(优先全局固定 `/tmp/compound_paddle_url.txt`,绕入库后台线程读不到 os.environ/gettempdir/BRAIN_DATA 的谜题)→curl worker→成功ocr:paddle失败回落rapidocr。sidecar_main._spawn_paddle_worker 落盘 /tmp+BRAIN_DATA+gettempdir。铁证:mac2简单表格图 method=ocr:paddle+完整`<table>`HTML。**paddle首次真正在图片入库发挥**(之前HD版图片入库其实都rapidocr、paddle白装)。

## 七、★ceshi复杂图 paddle 结论(重要,推翻旧判断需谨慎)
- 简单表格图:paddle ~110s 走成、出`<table>`。
- 复杂大图(mac2 ~/hdinstall/ceshi.jpg,4.7M意大利签证表):**paddle在i5上极慢**,curl 600s还超时回落rapidocr;直测(不设超时)跑了 **>18分钟仍未返回**(内存峰值5970MB≈6G)。**正在重跑直测(bb7k9mwif,让它跑到底拿最终耗时,别再中途清)**。
- 16G内存峰值6G不OOM;**8G会OOM**→HD定16G门槛正确。
- 初步结论:复杂大图paddle在i5实用性差(极慢),现有"能则paddle不能则回落rapidocr"降级合理(600s超时);"2小时不可用"数量级夸张但方向对(复杂图确实慢+8G OOM)。★最终耗时等 bb7k9mwif 直测结果确认。

## 八、★关键环境/坑
- 代理:云翼clash **18780**。github/gh/git **必须走代理**(直连000);github节点反复抽风(SSL/000)→切节点或等;OSS/localhost不走代理。
- 下载artifact:azure单连接易断→**mac2 `curl -sL -C -`循环续传**(经api.github.com重定向拿新SAS,不并发);gh偶发EOF重试。
- **macOS TCC**:mac2 ~/Desktop、~/Documents、~/Downloads SSH(非GUI)读不了(Operation not permitted,cp也拒)。测试文件放 **~/hdinstall/ 或主目录根**。真实GUI选文件不受限。
- **curl大文件upload**:-F@大文件默认Expect:100-continue,uvicorn响应差→只发header(size_up=146)传不上。SSH脚本curl大图要防(真实浏览器GUI无此问题)。
- mac2 SSH偶发拒绝→重试;选项必须 -o PubkeyAuthentication=no -o PreferredAuthentications=password -o NumberOfPasswordPrompts=1 -o ProxyCommand=none。长SSH(下载/装)易被本机SIGKILL→**mac2 nohup本地跑+轮询结果文件**。
- OSS欠费(已解决):模型(bge-m3 2.3G+sense-voice 0.9G)托管阿里云OSS(worldmonitor-downloads桶),曾UserDisable403→新装下不了模型。用户已充值。★bge-m3单OSS源,建议加备用源。
- Windows nsis装:SSH静默 `/S` 装不成(exit0但没装),需**用户GUI双击 C:\Users\Qingshi\compound-setup.exe**。
- 拿sidecar端口:lsof -nP -iTCP -sTCP:LISTEN|grep compound-(别grep [0-9]+$,撞resource_tracker)。
- HD sidecar启动~70s(paddle预加载12模型);HD app名"Compound 高精版.app",bundle id com.compoundtome.desktop.hd。

## 九、待办
1. #8/#9:重新构建三端(0351db0已在main)→装→Windows GUI验#8(支付宝弹页且不缺参数)/#9(导出PNG雷达居中)
2. ceshi直测(bb7k9mwif)最终结果→定复杂图策略(大概率接受rapidocr降级)
3. #5 微信助手向导钥匙数(下一批)
4. 新任务:会员价改价+注册免费10天(改云端106账号中心)
5. bge-m3加OSS外备用源防欠费
6. 文档补:建议客户 model用pro(质量)/fast用flash(快速)

## 十、构建/触发
- workflow:build-mac-intel.yml(lite)/build-mac-intel-hd.yml(HD)/build-windows.yml
- push自动lite+win;HD手动 `gh workflow run build-mac-intel-hd.yml --ref main`
- ★docs改动别push(触发重复构建)。构建~35-40min(HD最慢)。取消:`gh run cancel <run_id>`
- 三端下载装脚本模式见 /tmp/rel/(dl_lite*.sh/resume*.sh/hd_final*.sh),mac2用nohup本地跑

## 十一、凭据(明文见 memory compound_desktop_realmachine_test_2026-09-07)
mac2=zhaojue@172.16.16.172 / Win=Qingshi@172.16.17.175 / 密码都 qingshi@123 / 火山key见memory(直连不走代理) / 测试号17274630436密码Reset@2027xyz

## 十二、新增待办(2026-09-08晚·用户提)
- ★**客户端加"高精OCR处理中"提示**:复杂图/大图 paddle 在 i5 上极慢(实测4.7M签证表>10-18分钟),入库进度长时间停在ingesting、用户易以为卡死。应在前端入库进度(job phase=ingesting且走paddle时)显示"高精OCR处理中,复杂图较慢(可能数分钟),请耐心等待",或HD版单独标注。位置:前端Ingest/入库进度组件 + 可配合 ingest.py 走paddle时的进度回调。
