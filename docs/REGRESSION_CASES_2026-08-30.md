# 回归测试用例(逐 tab 逐功能 · 细颗粒)2026-08-30

> 用户令:审计每一个 tab,把回归 case 做到足够细致。**11 个导航 tab + 账户/设置全是真功能**(含冥想)。
> 每条 case:功能 · 前置条件 · 操作步骤 · 预期结果 · 验证点。★=关键路径 / ⚠=易出问题点。
> 冥想说明:歌=客户端给词/数据→106 用 302 key 生成→客户端展示播放,是完整真实业务流。

---

## TAB 1:入库(Ingest)

### 1.1 选文件入库
- 前置:已登录
- 步骤:入库页 → 点「选择文件」→ 选 1 个 PDF → 确认
- 预期:出现进度条 → 完成 → 文库里出现该文件,可搜到内容
- 验证点:/api/upload 返 job_id;/api/job 轮询到 done;pages 表有该 doc 的页;FTS 搜正文命中

### 1.2 选文件夹入库
- 前置:已登录;准备一个含混合格式(pdf/docx/图片/mp3)的文件夹
- 步骤:点「选择文件夹」→ 选文件夹 → 确认
- 预期:文件夹内所有支持格式并发上传 → 各自入库;不支持的被前端过滤
- ⚠ 验证:大文件夹(10+)进度正确;各格式走对提取路径(method 字段)

### 1.3 多格式覆盖(逐格式各 1)
- 文档:pdf/epub/docx/pptx/xlsx/md/txt/csv/json/html/eml/mbox → 入库后搜到正文
- 图片 OCR:png/jpg/webp/bmp/gif/**heic** → 入库 method=ocr:rapidocr,识别出文字
- 音频:mp3/wav/m4a → method=asr:sensevoice,转写出文字
- 视频:mp4/mov → 抽音轨转写 + 关键帧 OCR
- ⚠ HEIC 必须成功(iPhone 默认格式);azw3 需 mobi 转换

### 1.4 URL 抓取
- 步骤:入库框粘贴网址 → 回车/点「抓取入库」
- 分支:①普通网页→提取正文 ②视频站(B站/YouTube)→yt-dlp 下载转写 ③直链 PDF/音频→按 Content-Type 入库
- ⚠ 验证:SSRF 防护(粘 http://127.0.0.1 应被拒);视频 >800MB 会截断

### 1.5 ★OCR 后端切换(含 T430)
- **auto**(默认):数字版走文字层,扫描页自动 rapidocr
- **rapidocr**:强制本地 OCR
- **★t430**:前置 = T430 服务(172.16.11.199)在;配 ocr_url/T430_OCR_URL → 扫描页 POST 到 T430
  - ⚠ 关键验证:T430 连不上时 method=t430-unreachable,页内容为空(不自动回落本地)—— 确认这个行为,用户要知道
- 步骤:上传同一扫描 PDF,分别用 auto / rapidocr / t430,对比 method 和识别结果

### 1.6 音视频解析深度
- 前置:准备多人对话音频
- 预期:SenseVoice 转写逐字;≥2 人时说话人分离标「说话人N」;视频关键帧 OCR 出画面文字
- ⚠ 模型缺失时优雅降级(不崩,但用户可能不知为何空)

### 1.7 ★定期更新固定文件夹(autosync)
- ⚠⚠ 当前状态:**前端 checkbox 被 `{false &&}` 隐藏 + 后端无实现**。回归时确认:该功能是否要做?现状=不可测(空壳)。若要上线需 Tauri fs-watch + 后端 watch 线程。**这条是产品决策项,不是 bug 回归。**

### 1.8 微信实时同步(入库页内)
- 见 TAB「微信助手」完整流程(下载→装→抓密钥→监控→handoff→入库)

### 1.9 iOS 历史导入
- 前置:iPhone 连接 + 信任 + libimobiledevice
- 步骤:入库页 iPhone tab → 开始导入 → 备份→抽微信→认人→入库
- 已验证:B 上 39/39 会话;进度五段式

---

## TAB 2:问答(Home)

### 2.1 ★卡片 CRUD 全链
- 新建目标/日记/笔记/任务(4 类各测)→ 填标题内容 → 保存
- 保存后:联想历史(相关资料出现);卡片进列表
- 卡片详情:标记完成(状态变)/编辑(改标题内容)/删除(消失)
- 卡片问答:在卡片里问 → LLM 基于知识库答 + 出处
- 验证:/api/card CRUD;/api/card/{id}/related;删后列表空

### 2.2 全局问答
- 底部输入框问 → LLM 答 + 【来源N】→ 点来源跳文档
- 多轮追问(带 history)
- 对话历史:清空/全屏
- ⚠ 需 LLM key

### 2.3 今日发现 / 今日新闻
- 今日发现:本地 LLM 主动推荐(基于数据)
- 今日新闻:LLM 筛选(源可能来自云端 WM/serper)
- 空态:无数据时的引导

### 2.4 产出交付物(GenBar)
- 选主题 → 生成 PPT/Word/Excel → 预览 → 下载
- ★验证:立刻下载有内容(PPT 非空 deck 回归)
- ⚠ 需 LLM key;3 个连续生成注意超时

---

## TAB 3:探索(Explore)

### 3.1 文档星系 / 聊天星海
- 文档 tab:文档星系力导图渲染;聊天 tab:主题星云
- 前置:嵌入完成(bge-m3)
- 验证:/api/graph /starmap /chat_galaxy /chat_topic_galaxy 有节点

### 3.2 搜索飞向
- 搜索框输词 → 命中节点高亮 + 视角飞向

### 3.3 颜色分组
- 语义簇 / 文件类型 两种着色切换;颜色组展开收缩;点组隐藏/钻入

### 3.4 12 项调参(样式+动画)
- 辉光/节点/散开/连距 4 滑杆实时生效;2D/3D 切换;银河/星云等预设切换
- ⚠ 这些是纯前端交互,需 CDP 逐个点验证不崩 + 视觉正确

---

## TAB 4:画像(Persona)

### 4.1 生成个人画像
- 前置:有数据(文档/卡片/微信)+ LLM key
- 步骤:画像页 → 首次自动生成 or 点「重新生成」
- 预期:一句话定位 + 标签 + 领域权重 + 思维风格 + 价值观 + 好奇心,每条有据(反巴纳姆)
- ⚠ 无数据/无 key:友好空态引导;emb 何时生成需确认(影响姻缘匹配)
- 验证:/api/persona;personas 表有 data

---

## TAB 5:人脉(Relationships)

### 5.1 关系卡
- 前置:有微信数据 + LLM key(bg-analyzer 生成)
- 预期:每位联系人一张卡(身份/事实/未了结/人情)
- 「重新分析」触发生成

### 5.2 问TA / 见面简报 / 深聊
- 卡片上点 问TA/见面简报/深度分析 → AskDrawer 出对应内容
- 深聊一层 /api/relationships/deepen(⚠ 之前 -1 超时,复测)

### 5.3 关系图 / 时间线 / 找路径
- 关系图(rel_graph)、群图(group_graph)、关系时间线(relation_timeline)、找路径(rel_path a→b)

### 5.4 删关系卡(⚠ 破坏性)

---

## TAB 6:雷达(Radar)

### 6.1 六层逐个(前置:bg-analyzer 已跑出 chat_intel)
- 承诺雷达:欠的/等的承诺,到期排序;清除某条
- 供需撮合:供给×需求跨人匹配;「怎么牵线」
- 降温预警:互动变冷(纯时间戳算,不需 LLM);排序
- 人情待还:谁帮过我/暖场话题
- 沉默线索:21天+未联系有未了结;「看能否复活」
- 数字台账:报价/额度/期限结构化
- ★已验证:B 上 bg-analyzer 自动跑到 100%,六层有真数据

### 6.2 AI 动作按钮
- 催一下/怎么牵线/想开场白/看能否复活 → 都调 ask 出建议

---

## TAB 7:洞察(Insights)

### 7.1 四项逐个(前置:数据 + 部分需 LLM)
- 联系人画像(network_portrait):人脉网络图+领域分组+实体标签云
- 关系资产负债(balance):升温/降温/流失/新增(纯统计)
- 业务全景(panorama):按事/项目聚合(需 kb_entities)
- 沟通体检(checkup):谁在等你回/你对谁变冷(纯统计)
- 重新生成;标签云点击问;联系人点击

---

## TAB 8:好友(Friends)★多用户

### 8.1 前置:≥2 个注册账号,各自有画像
### 8.2 加好友:发现抽屉搜人 → 加 → 双向 friendships;对方也自动加
### 8.3 删好友:双向删除
### 8.4 ★算姻缘:点好友卡 → /api/match → LLM 出契合报告(6维雷达/恋爱视角/关系原型)
- ⚠ 需双方有画像;emb 填充影响精度
### 8.5 注册信息展示:真实填写(users2)优先于 AI 推测(personas),标「本人填写/AI推测」
- ⚠ bio 收集了但当前没展示 —— 确认是否要展示
### 8.6 好友星云 StarCloud:核心/中圈/星尘/命定星;搜好友;头像

---

## TAB 9:冥想(Life)★真功能

### 9.1 一生故事
- 前置:LLM key + 画像 + 数据
- 步骤:冥想页 → 自动/触发生成一生故事(读画像→脚本 30-42 镜头)
- 预期:脚本出;分镜图/图生视频陆续补(重生成走 106/云)
- 验证:/api/lifestory

### 9.2 ★一生歌全流程(客户端给词 → 106 用 302key 生成 → 客户端展示)
- 前置:LLM key + 画像
- 步骤:生成词稿(lifesong)→ 成曲(song/make,词给 106/302 生成)→ 轮询(song/status)→ 完成
- 预期:出一首专属歌,可播放;月度幂等(同月不重复)
- 验证:/api/lifesong /song/make /song/status;themes/ 出 mp3

### 9.3 播放/专辑/下载
- 黑胶播放器:播放/暂停/seek;频谱可视化;歌词同步高亮
- 专辑切换;下载 MP3
- 验证:/api/mylibrary 列出歌+片;/api/theme/{f} 出音频

### 9.4 旁白 TTS(edge-tts 离线)
### 9.5 空态引导:无 key/数据/画像 → 去配置/去导入/去生成画像

---

## TAB 10:文库(Library)

### 10.1 学科/类型 tab 切换;分类筛选;搜文件名;文档卡点击打开
- ⚠ 学科分类首次冷启 127-197s(KMeans+LLM命名),有缓存后秒回
- 验证:/api/library 返 documents+topics+ftypes

---

## TAB 11:说明(Help)

### 11.1 纯静态:数据源导入指南 / 下载助手按钮 / iPhone 图文 / 状态徽章释义 / 每 tab 说明
- 验证:各下载按钮可下(走本机 /dl);指南弹窗

---

## 账户 / 设置(左下角)

### A.1 ★注册:填手机号→获取验证码→填资料(昵称/性别/年龄/星座/MBTI/bio)→注册
- ⚠ 需真短信(云 SMS_DEV=0);资料存本地 users2
### A.2 登录:验证码登录 / 密码登录
### A.3 忘记密码 → 验证码重置;改密码(强度校验8位+2类+黑名单)
### A.4 改基本信息 update_profile;头像上传(裁256×256压缩)
### A.5 ★AI 模型选择:11 厂商预置 → 选厂商自动填模型 → 填 key → 质量/快两档 → 接口地址 → OCR地址 → 保存 → 测连通
### A.6 账户:试用倒计时/订阅;退出登录
- ⚠ 跨设备:资料/头像/AI key 只本地,换设备丢(确认是否要云同步)

---

## 阅读器 / 发现铃(通用)

### R.1 阅读器:打开文档/微信聊天;分页加载更多/更早;语义相关;问TA/见面简报/深度分析/产出文档;音视频 AI 纪要(media_structure)
### R.2 发现铃:主动发现流(降温/等回/承诺到期/供需);问大脑/看聊天/清除

---

## 回归执行顺序(清数据后)
1. 清测试账号数据(或用独立测试账号)
2. TAB1 入库全格式 + OCR后端 → TAB10 文库看到 → TAB2 问答/卡片/产出
3. bg-analyzer 跑出 → TAB6 雷达 / TAB7 洞察 / TAB5 人脉 / TAB4 画像
4. TAB3 探索星图 → TAB9 冥想(词→歌→播放)
5. 多账号 → TAB8 好友/姻缘
6. 账户全流程(需真短信)
7. 前端 425 交互点 CDP 逐页逐钮
