# 第二大脑桌面客户端 · 交接文档 2026-09-07

> 上一会话结束时的完整状态。新会话从这里接手。

## 一、代码状态（compound-desktop 仓,main 分支）

**已提交推送、三端构建 success 的最新版 = commit `d06a0fd`**（含下面 4 个 bug 修复）。
两仓库都：本地=远端、无未提交（除非下面标注）。

### 本次改的 4 个 bug（★关键：只有 #3 真机验证过，#1/#2/#4 未真机验证）
| # | bug | 改动文件 | 真机验证状态 |
|---|---|---|---|
| 1 | 设置里填 key 必须先保存才能点"测试连通" | llm.py(chat/test_key 加 cfg_override)、app.py(/api/settings/test 收 body)、Settings.jsx、api.js | ❌ **未真机验证** |
| 2 | 豆包默认模型 doubao-pro-32k 已下线(404) | llm.py PROVIDER_DEFAULTS、Settings.jsx PROVIDERS(改 doubao-seed-2 系列+alts+选型建议) | ⚠️ 仅命令行直打火山API验过5模型通，客户端设置里**未验** |
| 3 | **bg-analyze 格式化 bug→无限空转烧CPU 300%→客户端卡死** | app.py(pend/pend2 的 `"...LIKE '微信_与%'...LIMIT %d" % _AB` 改参数化 `LIMIT ?`) | ✅ **已真机验证**（Windows sidecar 空闲 0% CPU，修复前 300%）|
| 4 | 图片OCR每次 new RapidOCR 重加载模型 | media_ingest.py(_rapidocr_engine 单例) | ❌ **未真机验证** |

**#3 是本会话我自己改"消息数门槛"时引入的 P0**，它导致"开客户端就烧CPU/卡死"，也是之前 Windows"开客户端就烧CPU"的真根因。这个已修好并验证。

### 待修但还没动的 bug
- **#5 微信助手向导显示"抓到1把"吓人**：退登重登只新抓1把候选(cand_keys.txt=1)，向导 `_wpoll` 显示的是**本次候选数**而非**累计可用数**。实际 `keys_captured.json` 里 **19 把钥匙都在、没丢**，主面板 status() 数的是 keys_captured(19) 正常。修法：向导抓取中提示改成显示"累计可用N把(本次新增X)"，别只显本次候选。文件=~/wxkeys/panel.py 的 capture()/_wpoll。**注意**：capture() 开头我加了 `os.remove(cand_keys.txt)` 清候选（修跳步用），退登重登只抓1把可能与此有关也可能是微信只重开1个库，需排查。

## 二、三台机器真实状态（★重要）
| 机器 | 连接 | 装机 | 版本 | 测试情况 |
|---|---|---|---|---|
| **本机 Mac(8G Intel)** | 本地 | ✅ app在(/Applications/Compound.app 0.1.0) 但 sidecar 常没自动起 | lite d06a0fd | 抢窗口测过8个API 200 |
| **Windows(8G,Qingshi@172.16.17.175 密码【问用户】)** | SSH(需 env -u 清代理 + ProxyCommand=none) | ✅ 装好(干净) | lite d06a0fd | ✅#3 CPU验过; #1/#2/#4/#5 **未验完** |
| **mac2(16G Intel,zhaojue@172.16.16.172 密码【问用户】)** | SSH(仅密码:-o PubkeyAuthentication=no -o PreferredAuthentications=password) | ❌ **未装新版**(HD zip被删腾空间了,要重下5G) | 旧版d06ba30还在 | 无 |

## 三、关键环境/凭据
- Windows token(账号18201972547,可能过期需重取): 从 `C:\Users\Qingshi\AppData\Local\com.compoundtome.desktop\EBWebView\Default\Local Storage\leveldb` 里 grep `eyJ...` (文件被占用需先 Copy-Item 到临时目录再读)
- 本机 token 同理在 `~/Library/WebKit/com.compoundtome.desktop/WebsiteData/LocalStorage/tauri_localhost_0.localstorage`(sqlite,SELECT hex(value) FROM ItemTable WHERE key='auth',UTF-16解码)
- 火山引擎(豆包)测试 key: 【问用户要,别写进文档/仓库】,base=火山方舟,**必须直连不走代理**,可用模型(带版本号): doubao-seed-2-0-mini-260428 / doubao-seed-2-1-pro-260628 / doubao-seed-2-1-turbo-260628 / doubao-seed-2-0-lite-260428 / glm-5-2-260617
- 构建 workflow: build-mac-intel.yml(lite) / build-mac-intel-hd.yml(HD) / build-windows.yml。触发: push 自动触发 lite+win; HD 要 `gh workflow run build-mac-intel-hd.yml --ref main`
- ★★下载 artifact 坑: 不要并发多进程 + 30s重连循环(会重复写坏文件 bad CRC)。**正解=单进程 no-proxy 直连**(env -u http_proxy... curl),实测 11MB/s。Azure blob 对单长连接限速但新连接快。gh api 有时 EOF,改 curl 直连 api.github.com 拿 artifact id。
- ★本机磁盘紧张(常只剩几G): HD zip 5G+dmg 3G 易撑爆→ENOSPC 连工具输出都写不了。装前先清 /tmp/rel。

## 四、测试用例文档
`docs/REGRESSION_FULL_2026-09-06.md`（~350条细粒度）：
- §0.5 本次交付改动专项验证(AI欠费提示/微信助手新向导/人脉卡消息数门槛)
- §0.6 高频新建(目标/日记/笔记/任务)
- §1-§4 全量(安装/账号/入库全品种/微信/问答搜索/人脉雷达/探索冥想文库/设置多模型/样式)

## 五、已验证 OK 的（有据）
- ✅ #3 CPU修复(Windows 0%)
- ✅ 9格式入库内容全对(本机: txt/md/csv/html/eml/docx/xlsx/pptx/png,png OCR识别准)
- ✅ 火山5模型命令行直调通
- ✅ 本机8个功能API 200(relationships/persona/library/today/cards/realtime/ingest/autosync)
- ✅ Apple Silicon: x86客户端经Rosetta能跑(GitHub M1 runner: sidecar health200,壳进程起,#3 CPU 0%)→ M芯片装Mac Intel版即可,不出arm原生
- ✅ HD高精OCR: 30G runner实测能识别(表格还原HTML表);但真实微信图在Intel CPU上2小时跑不出=实际不可用→已决定主页下载处加"最低应用环境"说明(需16G+较新CPU),不改代码

## 六、新会话建议第一步
1. **接着"把 Windows 完整测完"**(用户上条指令): Windows已装好新版,验 #1(填key直接测)/#2(切豆包模型下拉)/#4(图片入库OCR)/#5(助手钥匙数显示) + §0.5/§0.6/入库全品种。**建议让用户在Windows界面点GUI交互项(填key测连通/切模型/新建/助手向导),Agent SSH看后端日志兜底——比纯远程戳API高效**。
2. mac2 HD 需重下5G安装(单进程no-proxy)后测。
3. 本机 sidecar 没自动起,重启客户端。
4. 全测完把 #5 + 其它新bug统一修 → 再构建。

## 七、上会话教训(避免重犯)
- 别反复说"已改好"混淆"改代码"vs"真机验证"——未验证就明说未验证。
- 别陷在下载/token/SSH反复戳里空转;GUI交互项让用户点最快。
- 下载用单进程no-proxy,别并发+重连(坏文件)。
- 装机前清磁盘。
