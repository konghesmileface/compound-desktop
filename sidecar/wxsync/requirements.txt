# wxsync 客户端依赖
# ------------------
# 核心逻辑(config/status/uploader/import_iphone/realtime_poll)只用 Python 标准库
# (sqlite3 / urllib / hashlib / subprocess),不需要第三方 pip 包。
#
# 因此本文件为空(占位)。若后续要打包成桌面客户端(Tauri/PyInstaller),
# 再在这里加打包相关依赖。
#
# 系统层依赖(不是 pip,用户/CI 需另装):
#   - Python 3.9+
#   - iPhone 导入路线:libimobiledevice (提供 idevice_id / idevicebackup2)
#       macOS:  brew install libimobiledevice
#       Windows:用 iTunes 或 libimobiledevice 的 Windows 构建
