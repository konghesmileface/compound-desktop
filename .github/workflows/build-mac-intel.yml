name: Build Mac Intel 轻量版

# 轻量版(rapidocr v6 打进包;bge-m3/SenseVoice 0.2.0 起不打包→首启 model_bootstrap 从 OSS 下,包瘦到几百M)。
# ★用 macos-latest(arm,不排队)+ Rosetta 跑 x86_64 Python 出 Intel sidecar;
#   Tauri 交叉编译到 x86_64。避开 macos-13 死排。
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'sidecar/**'
      - 'src-tauri/**'
      - 'frontend/**'
      - '.github/workflows/build-mac-intel.yml'

jobs:
  build:
    runs-on: macos-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-apple-darwin

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: './src-tauri -> target'
          cache-on-failure: true

      - name: 装 x86_64 Python(经 Rosetta 出 Intel sidecar)
        run: |
          curl -fsSL -o /tmp/python.pkg https://www.python.org/ftp/python/3.10.11/python-3.10.11-macos11.pkg
          sudo installer -pkg /tmp/python.pkg -target /
          echo "PY=/Library/Frameworks/Python.framework/Versions/3.10/bin/python3" >> "$GITHUB_ENV"

      # ★0.2.0 瘦身:bge-m3(2.3G)/SenseVoice(0.9G)不再打进包(对齐 Windows 已验证路)。
      #   首启 model_bootstrap 从 OSS 下到数据目录;版本更新只换程序壳,模型不重下。
      - name: 下音视频入库模型(不含SenseVoice) + Mac ffmpeg
        working-directory: sidecar
        run: |
          set -e
          mkdir -p models bin
          # silero VAD ★必须 v4(sherpa-onnx 1.11.1 不认新版 silero,报 Unsupported silero vad model)
          curl -fsSL -o models/silero_vad.onnx https://github.com/snakers4/silero-vad/raw/v4.0/files/silero_vad.onnx
          # 说话人分离:pyannote 分割 + 3dspeaker 声纹
          curl -fsSL -o /tmp/seg.tar.bz2 https://github.com/k2-fsa/sherpa-onnx/releases/download/speaker-segmentation-models/sherpa-onnx-pyannote-segmentation-3-0.tar.bz2
          tar xjf /tmp/seg.tar.bz2 -C models/
          curl -fsSL -o models/3dspeaker_speech_eres2net_base_sv_zh-cn_3dspeaker_16k.onnx https://github.com/k2-fsa/sherpa-onnx/releases/download/speaker-recongition-models/3dspeaker_speech_eres2net_base_sv_zh-cn_3dspeaker_16k.onnx
          # Mac Intel 静态 ffmpeg(evermeet.cx)
          curl -fsSL -o /tmp/ff.zip https://evermeet.cx/ffmpeg/getrelease/ffmpeg/zip
          unzip -o /tmp/ff.zip -d bin/ && chmod +x bin/ffmpeg
          ls -la models/ bin/

      - name: 冻结后端 sidecar(x86_64,内含 rapidocr v6)+ selftest
        working-directory: sidecar
        run: |
          arch -x86_64 "$PY" -m pip install -r requirements.txt pyinstaller
          # ★cryptography 含 Rust 原生扩展(_rust.abi3.so):universal2 Python 下 pip 常误装 arm64 slice,
          #   PyInstaller 交叉打 x86_64 时报 IncompatibleBinaryArchError。先卸载再用 --platform 强制拉含 x86_64 的 wheel。
          SITE=$(arch -x86_64 "$PY" -c "import site;print(site.getsitepackages()[0])")
          arch -x86_64 "$PY" -m pip uninstall -y cryptography || true
          arch -x86_64 "$PY" -m pip install --no-deps --only-binary=:all: --platform macosx_10_12_universal2 --platform macosx_10_9_x86_64 --target "$SITE" cryptography
          file "$SITE"/cryptography/hazmat/bindings/_rust.abi3.so
          arch -x86_64 "$PY" -m PyInstaller --noconfirm compound-sidecar.spec
          file dist/compound-sidecar/compound-sidecar
          ./dist/compound-sidecar/compound-sidecar --selftest

      - name: Tauri 打 Intel .app/.dmg(交叉编译 x86_64,内嵌 x86_64 sidecar)
        env:
          VITE_TAURI: '1'   # ★桌面包:vite base 用相对路径(原内联在 beforeBuildCommand,Windows cmd 不认→改 env 跨平台)
        run: |
          npm install
          npm run tauri build -- --target x86_64-apple-darwin

      - name: 上传产物
        uses: actions/upload-artifact@v4
        with:
          name: compound-mac-intel-lite
          path: src-tauri/target/x86_64-apple-darwin/release/bundle/dmg/*.dmg
          if-no-files-found: warn
